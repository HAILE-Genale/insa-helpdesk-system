spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/insa_helpdesk_v2
    username: helpdesk_user
    password: helpdesk_8001
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: false

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}
