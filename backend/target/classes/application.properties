spring.application.name=helpdesk

# --- Database (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/helpdesk
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=8080

# --- Frontend origin for CORS ---
app.cors.allowed-origin=http://localhost:3000

# --- Email intake (fill in when wiring FR-010's email channel) ---
# spring.mail.host=imap.yourmailserver.com
# spring.mail.port=993
# spring.mail.username=helpdesk@yourcompany.com
# spring.mail.password=change_me
# spring.mail.properties.mail.imap.ssl.enable=true
