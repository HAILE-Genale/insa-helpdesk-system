spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/insa_helpdesk
    username: postgres
    password: 4neverever
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: false

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}
